services:
  admin:
    image: ghcr.io/podvysotskyi/l2-admin:${L2_ADMIN_IMAGE_TAG:-latest}
    ports: ['3002:3002']
    environment:
      NUXT_PUBLIC_ADMIN_API_BASE: ${NUXT_PUBLIC_ADMIN_API_BASE:-http://localhost:5201}
